---
name: no-period
description: Valid length and inline format but missing the required trailing period so the checker should reject this description for formatting consistency
---

# No period fixture
